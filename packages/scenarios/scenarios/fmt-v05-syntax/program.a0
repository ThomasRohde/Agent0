let base = { x: 1 }
let spread = { ...base, y: 2 }
let items = [1, 2, 3]
let evens = filter { in: items, as: "n" } {
  return n % 2 == 0
}
let total = loop { in: 0, times: 3, as: "acc" } {
  return acc + 10
}
return total
