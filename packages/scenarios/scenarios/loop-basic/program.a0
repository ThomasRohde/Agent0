let counter = loop { in: 0, times: 5, as: "x" } {
  return x + 1
}

let state = loop { in: { total: 0, count: 0 }, times: 3, as: "s" } {
  return { total: s.total + 10, count: s.count + 1 }
}

let unchanged = loop { in: 42, times: 0, as: "x" } {
  return x + 1
}

return { counter: counter, state: state, unchanged: unchanged }
